const addClassList=(nameclass)=>{
    document.body.classList.add(nameclass)
}
const removeClassList=(nameclass)=>{
    document.body.classList.remove(nameclass)
}
export {addClassList,removeClassList}