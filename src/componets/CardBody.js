import { Card, Grid, Row, Text } from "@nextui-org/react";
import { useState } from "react";
const CardBody=(props)=>{

    const [dataCard,setDataCard]=useState(props.datacard?props.dataCard:[])

    return(
        <div></div>

    )
}
export default CardBody;